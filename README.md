# Stripe Elements BigCommerce Integration

Everything in this project is based on https://docs.stripe.com/payments/quickstart


## BigCommerce Configuration

1. You'll need to implement `cornerstone_customization/checkout` into your BigCommerce theme as well as add your Stripe publishable key.

## Server Configuration

1. You'll need to `cp .env.example .env` and configure variables
